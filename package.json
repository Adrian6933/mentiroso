{
  "name": "mentiroso",
  "private": true,
  "version": "0.1.0",
  "description": "El Mentiroso — juego del impostor de famosos. Modo local y multijugador online.",
  "workspaces": [
    "shared",
    "web",
    "server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:web dev:server",
    "dev:web": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace web && npm run build --workspace server",
    "test": "npm run test --workspace shared"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
